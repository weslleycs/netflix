// Crie um objeto `Cart` que suporte adicionar itens, remover itens, calcular o preço total e contar itens. Por exemplo:
// `cart = new Cart()`
// `item1 = new Item("Apple", 2)`
// `item2 = new Item("Banana", 3)`

// `cart.addItem(item1)`
// `cart.addItem(item2)`

// `cart.getTotal()` # deve retornar 5
// `cart.getItemCount()` # deve retornar 2

// `cart.removeItem("Apple")`
// `cart.getTotal()` # deve retornar 3

// `cart.removeItem("Orange")` # deve lançar um erro (item não encontrado)

// Se você tentar remover um item que não existe, você deve lançar um erro!

console.log('hola');
