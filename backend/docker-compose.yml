services:
  mysql:
    image: mysql:8.0
    container_name: mysql-netflix
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: netflix
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
